# 20 - Sistema de Autenticación (JS)

Descripción
- Ejemplo de backend ligero para registro y login con JWT.

Tecnologías
- Node.js, Express, bcryptjs, jsonwebtoken

Cómo ejecutar
1. `npm install`
2. `npm start`
3. Probar con `POST /register` y `POST /login` (JSON body: `username`, `password`).
